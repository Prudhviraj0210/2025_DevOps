# Linux Basic Commands

## 6. Types of Files

### File Types in Linux
- **Normal File**: `-`
- **Directory**: `d`
- **Link File**: `l` (shortcut)
- **Block File**: `b` (e.g., hard disk, floppy disk)
- **Character File**: `c` (e.g., keyboard, mouse)

### Symbolic Links

#### Types of Links
1. **Soft Link** (Shortcut):
   - Created using `ln -s`.
   - Can be created across partitions.
   - Inode number differs from the source file.
   - Broken if the source file is deleted.
   ```bash
   ln -s source_file destination
   ```

2. **Hard Link** (Backup):
   - Created using `ln`.
   - Cannot be created across partitions.
   - Same inode number as the source file.
   - Retains data even if the source file is deleted.
   ```bash
   ln source_file destination
   ```

---

## 7. Regular Expressions, Pipelines & I/O Redirection

### Using `grep` Command
- **Basic Syntax**:
  ```bash
  grep pattern filename
  ```
- **Examples**:
  - Search for a word:
    ```bash
    grep root /etc/passwd
    ```
  - Ignore case sensitivity:
    ```bash
    grep -i kernel ktfile
    ```
  - Display lines before and after a match:
    ```bash
    grep -nA2 wheel /etc/group
    grep -nB2 wheel /etc/group
    ```
  - Exclude lines with a pattern:
    ```bash
    grep -v kernel ktfile
    ```
  - Highlight matches in color:
    ```bash
    grep --color root /etc/passwd
    ```

### Combining `grep` with Other Commands
- Use `|` to pipe commands:
  ```bash
  ls -l | grep ktfile
  cat ktfile | grep kernel
  ```

---

### Filter Commands
- **`less`**: View output line by line or page by page.
  ```bash
  less /etc/passwd
  ```
- **`more`**: Similar to `less` but simpler.
  ```bash
  more /etc/passwd
  ```
- **`head`**: Display the first 10 lines of a file.
  ```bash
  head /etc/passwd
  head -n 5 /etc/passwd
  ```
- **`tail`**: Display the last 10 lines of a file.
  ```bash
  tail /etc/passwd
  tail -n 5 /etc/passwd
  ```

---

## 8. File Permissions

### Access Levels
- **User Levels**:
  1. **Owner**: User who owns the file.
  2. **Group**: Group associated with the file.
  3. **Others**: All other users.

- **Access Modes**:
  1. `r`: Read-only.
  2. `w`: Write, edit, delete, or append.
  3. `x`: Execute or run the file.

### Setting Permissions
- **Symbolic Method**:
  ```bash
  chmod u=rwx,g=rw,o=r filename
  chmod u+x filename
  chmod go-wx filename
  ```
- **Numeric Method**:
  ```bash
  chmod 764 filename
  chmod 777 filename
  ```
  - Values:
    - `r = 4`
    - `w = 2`
    - `x = 1`

### Viewing File Permissions
- Use `ls -l`:
  ```bash
  ls -l filename
  ```

### `umask`
- Default permissions are determined by the `umask` value.
- **Default Values**:
  - Files: `644`
  - Directories: `755`
- **Modify `umask`**:
  ```bash
  umask 002
  ```

---

This section includes Linux file types, regular expressions, I/O redirection, and file permission management from pages 41 to 55.
